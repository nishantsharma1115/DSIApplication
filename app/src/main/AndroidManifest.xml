<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application.dsi">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.ContactUsActivity"></activity>
        <activity android:name=".ui.AboutUsActivity" />
        <activity android:name=".ui.ProductActivity" />
        <activity
            android:name=".ui.ApplyForDigitalCardActivity"
            android:label="Apply For Digital Card"
            android:parentActivityName=".ui.CustomerListActivity" />
        <activity
            android:name=".ui.EmployeeProfileActivity"
            android:label="Employee Detail" />
        <activity
            android:name=".ui.CustomerListActivity"
            android:label="Registered Customers"
            android:parentActivityName=".ui.UserDashboardActivity" />
        <activity
            android:name=".ui.CustomerRegistrationActivity"
            android:label="Customer Registration"
            android:parentActivityName=".ui.UserDashboardActivity" />
        <activity android:name=".ui.UserDashboardActivity" />
        <activity
            android:name=".ui.SignUpActivity"
            android:parentActivityName=".ui.LoginActivity" />
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>